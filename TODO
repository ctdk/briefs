- Read-write FUSE bridge (read-only already exists via briefs-utils).
- Broader test coverage (e.g., xfstests integration, crash-consistency tests)
- Annotate the source code thoroughly once things are more stable
- Move away from any deprecated kernel functions BrieFS may be using. Likely to
  come naturally as it's updated to work with newer kernels, but still needs
  calling out.
- [Done] Support large extended-attribute values via chained 4 KiB
  continuation blocks, with crash-safe journal replay and fsck validation.
  (Kernel commit `29121e6`, briefs-utils `042d09b`. xfstests xattr cluster
  020/037/062/066/070/097/103/117/337/377/403/454/631 passes 13/13.)
- [Done] Migrate mount path to fs_context so remount works; honor the
  existing "debug" flag (and "nodebug") and checkpoint the journal on
  read-only remount.
- [Done] chattr/lsattr inode flags (+S/+D/+i/+a/+d/+A), wired through
  `fileattr_get/set` so both `FS_IOC_{GET,SET}FLAGS` and
  `FS_IOC_FS{GET,SET}XATTR` work, with +S/+D flushing inode, journal and
  drive for metadata durability. (Kernel commit `38d57d0`; closes xfstests
  `generic/079 277 424 545 553 555 596 629`.)
- Make list of additional relevant mount options to honor, whether fs
  independent or unique to briefs. (Started: norecovery, errors=, debug/nodebug.)
- [Done] Add norecovery mount option: skip journal replay, require ro mount.
- [Done] Add errors=continue|remount-ro|panic mount option and central metadata
  write-error handler.
- [Done] fs_context-based mount/remount, debugfs toggle on remount.
- Revise briefs(5) man page.
- Quotas
- POSIX ACLs
- online resize (or any resize)
- extend inode map/add inodes to existing volume

Much further down the list, if ever, items:
- Optional data journaling
- reflink/COW, fscrypt, fsverity, casefolding
- build-in compression
